import React, { Component } from 'react';

class ToggleButtonComponent extends Component {
  render() {
    return <button>1</button>
  }
}

export const ToggleButton = withStyles({})(ToggleButtonComponent);